apiVersion: v1
kind: Service
metadata:
  name: spring-transaction-service
spec:
  type: NodePort
  selector:
    app: spring-transaction
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007